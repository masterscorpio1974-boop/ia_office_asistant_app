{
  "name": "ia-office-assistant",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'No build needed'",
    "sync": "npx cap sync",
    "android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2"
  }
}
